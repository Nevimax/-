package com.nevimax.nmynotes;import android.content.Intent;import android.os.Bundle;import android.util.Log;import android.view.View;import android.widget.ImageButton;import android.widget.TextView;import android.app.Activity;import androidx.appcompat.app.AppCompatActivity;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import java.util.List;public class MainActivity extends AppCompatActivity implements View.OnClickListener {    //    //BD//    private List<Employee> notes = new ArrayList<>();    private AppDatabase appDatabase;    private EmployeeDao notesDao;    RecyclerView recyclerView;    NoteRecyclerAdapter adapter;    ImageButton add;    List<Employee> noteModels;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        appDatabase = App.getInstance(this);        notesDao = appDatabase.employeeDao();        noteModels = notesDao.getAll(); // на этом моменте смерть        recyclerView = findViewById(R.id.recyclerView);        adapter = new NoteRecyclerAdapter(noteModels);        recyclerView.setAdapter(adapter);        recyclerView.setLayoutManager(new LinearLayoutManager(this));        recyclerView.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                Intent intent = new Intent(MainActivity.this, NoteActivity.class);                startActivity(intent);            }        });        recyclerView.setOnLongClickListener(new View.OnLongClickListener() {            @Override            public boolean onLongClick(View view) {                Intent intent = new Intent(MainActivity.this, NoteActivity.class);                startActivity(intent);                return false;            }        });        add = findViewById(R.id.buttonAdd);        add.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                Intent intent = new Intent(MainActivity.this, NoteActivity.class);                startActivity(intent);            }        });    }    @Override    protected void onStart() {        super.onStart();        Log.d("QWE","onStart()");//список обновляеться, сразу после жмяка на кнопку на НотАктивити, все 4 пункта        noteModels = notesDao.getAll();        adapter = new NoteRecyclerAdapter(noteModels);        recyclerView.setAdapter(adapter);        recyclerView.setLayoutManager(new LinearLayoutManager(this));    }    @Override    protected void onResume() {        super.onResume();        Log.d("QWE","onResume()");    }//commit    @Override    public void onClick(View view) {        switch (view.getId()){            case R.id.nameView:                Intent intent = new Intent(MainActivity.this, WeathActivity.class);                startActivity(intent);            break;        }    }}